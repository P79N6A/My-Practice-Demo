// 二叉树的节点
class TreeNode {
  constructor (value, left, right) {
    this.value = value
    this.left = left || null
    this.right = right || null
  }
}

class BinarySearchTree {
  constructor (arr) {
    let root = new TreeNode()
  }
}